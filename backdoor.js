/* TODO: Update what this scripts intention is
*/

/**
* @param {import(".").NS} ns Use just "@param {NS} ns" if editing in game
*/
export async function main(ns) {
  // TODO: Figure out error message "requires source file 4-1 to run"
  // for (let i = 0; i < hops.length; i++) {
  //     const server = hops[i];
  //     ns.print(`connecting to ${server}`);
  //     ns.connect(server); // Requires "Source-File 4-1 to run"
  //     ns.print(`connection established to ${server}`);
  // }

  // ns.print(`connecting to ${target}`);
  // ns.connect(target)
  // await ns.installBackdoor();
}